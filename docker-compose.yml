version: '3'

services:
  php:
      environment:
        - XDEBUG_SESSION=docker-server
      build:
        ./
      volumes:
        - ./:/var/www/html
        - ./xdebug.ini:/usr/local/ect/php/conf.d/xdebug.ini
      ports:
        - "8080:80"